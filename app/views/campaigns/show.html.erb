<!-- app/views/campaigns/show.html.erb -->
<h1><%= @campaign.name %></h1>

<h2>Candidates</h2>
<% @candidates_with_scores.each do |candidate| %>
  <p><%= candidate[:candidate] %>: <%= candidate[:score] %></p>
<% end %>

<p>Uncounted Messages: <%= @uncounted_messages_count %></p>
